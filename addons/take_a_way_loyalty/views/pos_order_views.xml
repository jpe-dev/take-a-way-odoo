<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <data>
    <!-- Commenté temporairement car les vues PoS ne sont pas accessibles
    <record id="view_pos_order_form_inherit_heure_prevue" model="ir.ui.view">
        <field name="name">pos.order.form.inherit.heure.prevue</field>
        <field name="model">pos.order</field>
        <field name="inherit_id" ref="point_of_sale.view_pos_pos_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='date_order']" position="after">
                <field name="heure_prevue" string="Heure prévue" widget="datetime"/>
            </xpath>
        </field>
    </record>
    -->

  </data>
</odoo> 