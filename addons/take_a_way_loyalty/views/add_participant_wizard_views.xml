<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_add_participant_wizard_form" model="ir.ui.view">
        <field name="name">add.participant.wizard.form</field>
        <field name="model">add.participant.wizard</field>
        <field name="arch" type="xml">
            <form string="Ajouter un participant à la mission">
                <group>
                    <field name="mission_id" readonly="1"/>
                    <field name="participant_id"/>
                </group>
                <footer>
                    <button name="action_add_participant" type="object" string="Ajouter" class="btn-primary"/>
                    <button string="Annuler" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>
</odoo> 